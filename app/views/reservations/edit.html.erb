<section class='edit-reservation-form'>
  <%= render partial: 'form', locals: {
    model: @reservation, 
    tables: @tables,
    submit_button_text: 'Update Reservation', 
    cancel_path: reservation_path(@reservation.id)
  } %>
</section>